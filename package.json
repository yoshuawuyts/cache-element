{
  "name": "cache-element",
  "version": "3.0.0",
  "description": "Memoize a bel element",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard *.js && npm run deps",
    "test:cov": "standard && npm run deps"
  },
  "repository": "yoshuawuyts/cache-element",
  "keywords": [
    "bel",
    "choo",
    "element",
    "thunk",
    "cache",
    "perf",
    "cache"
  ],
  "license": "MIT",
  "dependencies": {
    "nanocomponent": "^3.0.2"
  },
  "devDependencies": {
    "dependency-check": "^2.8.0",
    "standard": "^10.0.1"
  }
}
